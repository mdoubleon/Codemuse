# Web

> 注意：当前真正的浏览器 UI 位于 [src/codemuse/web/static/](../src/codemuse/web/static/)，由后端 `scripts/run_server.py` 直接 serve。
>
> 这个目录原计划放未来独立打包的 SPA（如 Vite/React 构建产物），目前保留作为占位。
> 启动 Web 界面请参考根目录 `README.md` 的 `Backend run` 一节。
